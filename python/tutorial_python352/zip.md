Cria uma lista de tuplas ao unir duas listas.

    >>> a = ['rodolfo','jessica','anderson']
    >>> b = ['calado','briguenta','gaiato']
    >>> list(zip(a,b))
    [('rodolfo', 'calado'), ('jessica', 'briguenta'), ('anderson', 'gaiato')]     
