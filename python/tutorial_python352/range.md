    #! /usr/bin/python
    # encoding: utf-8

    a = ['Mary', 'had', 'a', 'little', 'lamb']
    for i in range(len(a)):
        print(i, a[i])

'''
funcao range() lista uma sequencia de numeros passados
'''
